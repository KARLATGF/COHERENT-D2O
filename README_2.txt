After running simulation, do the following from sim main directory:

$ root -l scripts/load_simEvent.C 
$ .L scripts/forOlga.C
$ generatePlots()

This will generate the file root light_yield_deuterium_top.root, where the 2D histograms "hLYD" lives. Open the .root file and plot it!
